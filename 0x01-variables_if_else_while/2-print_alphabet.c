int main(void)
{
    char c = 'a';

    // Print the alphabet using a loop
    while (c <= 'z') {
        putchar(c);
        c++;
    }

    // Print a new line character
    putchar('\n');

    return 0;
}
